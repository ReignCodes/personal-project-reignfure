<%- include('../partials/header') -%> 
<%- include('../partials/nav') -%>
<main>
    <div class="container">
        <div class="contentContainer"></div>
            <h3>Community Fridges & Pantries</h3>
            <% for (resource of resources) {%>
            <div class="contentBox">
                <div class="eachResource">
                <!-- <a href="/resourceDetail/<%= resource._id %>"><img src="/images/<%= resource.image %>" width="200" /></a> -->
                <br>
                <p><span class="resource_name"> <%= resource.name %></span></p>
                <p><%= resource.city %>, <%= resource.state %></p>
                <p><a href="<%= resource.google_maps %>">Google Directions</a></p>
                <p><%= resource.type %></p>
                <p><%= resource.web %></p>
                <!-- what should this link be? -->
                <a class="details_link" href="/resource/<%= resource._id %>">More Info...</a> 
            </div> 
        </div>
        <% } %>
    </div>

    </div>
</main>
<%- include('../partials/footer') -%>