<% for (resource of resources) {%>
<tr>
    <td><%= resource.name %> </td>
    <td><%= resource.type %> </td>
    <td><%= resource.approved %></td>
    <td>  <button class="review-button"><a href="/adminConsole/resource/<%= resource._id %>">Review</a></button>
    </td>
    <td>
        <form class="tableForm" action="/resource/<%= resource._id %>?_method=DELETE" method="POST">
       <button class="review-button" type="submit">Delete</button>
        </form> 
    </td>
 </tr>
 <%}%>